import React from "react";
import Date from "./Date";
import Time from "./Time";

function Schedule() {
  return (
    <div>
      <h2>スケジュール</h2>

      {/* 1日分のスケジュールBOX&日付&削除ボタン&1つのスケシュール追加ボタン */}
      <Date />
      {/* 1つのスケジュールBOX&削除ボタン */}

      {/* 1日分のスケジュールBOX&日付&削除ボタン&1つのスケシュール追加ボタン */}
      {/* 1つのスケジュールBOX&削除ボタン */}
    </div>
  );
}

export default Schedule;
